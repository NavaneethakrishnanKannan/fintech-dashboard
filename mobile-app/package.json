{
  "name": "wealth-saas-mobile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo Add your deployed URL in capacitor.config.ts then run: npx cap sync",
    "android": "npx cap open android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^5.3.0"
  },
  "capacitor": {
    "platforms": ["android", "ios"]
  }
}
